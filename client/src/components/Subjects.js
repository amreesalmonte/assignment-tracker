import React, { Component } from 'react';

export default class Subjects extends Component {
    render() {
        return (
            <div>
                Subjects
            </div>
        );
    }
}